# MySQL (used by index forms, login, dashboard, admin lists, submit-email)
DB_HOST=localhost
DB_USER=
DB_PASSWORD=
DB_NAME=

# Optional: chatbot_lead.php mail()
MAIL_ADMIN=contact@example.com
MAIL_FROM=no-reply@example.com
